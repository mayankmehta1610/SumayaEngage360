# SumayaEngage360 - Video Walkthrough Script

**Tenant:** sumaya (demo)
**Roles covered:** PLATFORM_ADMIN, TENANT_ADMIN, HR, MANAGER, EMPLOYEE, INTERVIEWER, BGC_VENDOR, DEPARTMENT_HEAD
**Verification:** 839 of 3,000 workbook features are marked Done from executable evidence; unverified rows remain Not Started.

## ch01: Landing

**Screenshot:** `screenshots/01-landing.png`

Welcome to Sumaya Engage 360, an enterprise platform for hiring, onboarding, and managing the full employee lifecycle. This multi-tenant SaaS gives each company an isolated workspace. The landing page introduces the product and links to login and public careers.

## ch02: Careers Public

**Screenshot:** `screenshots/02-careers-public.png`

Candidates apply through public careers pages, one per hiring client. Each job card shows title, location, skills, and an Apply Now form. Applications flow directly into HR's pipeline without requiring a login.

## ch03: Login

**Screenshot:** `screenshots/03-login.png`

Staff sign in with tenant subdomain, email, and password. Platform administrators leave the tenant field empty. After authentication, users land in the application shell with modules filtered by their assigned roles.

## ch04: Tenant Admin Dashboard

**Screenshot:** `screenshots/04-tenant-admin-dashboard.png`

As a tenant admin, The dashboard aggregates live KPIs—open jobs, applications, headcount, and pending approvals—with tiles that drill into underlying data. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch05: Tenant Admin Reports

**Screenshot:** `screenshots/05-tenant-admin-reports.png`

From the tenant admin perspective: Reports provides a catalogue of KPI definitions. Select a report code, set filters, and run to generate on-screen analytics with export options. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch06: Tenant Admin Settings

**Screenshot:** `screenshots/06-tenant-admin-settings.png`

From the tenant admin perspective: Settings configures tenant-wide options: branding, approval thresholds, integrations, and feature toggles managed by tenant admins and HR. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch07: Tenant Admin Catalogues

**Screenshot:** `screenshots/07-tenant-admin-catalogues.png`

From the tenant admin perspective: Catalogues hold reference data seeded for the tenant—skills, document types, policy templates, and other master lists used across modules. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch08: Tenant Admin Requirements

**Screenshot:** `screenshots/08-tenant-admin-requirements.png`

From the tenant admin perspective: Requirements tracks implementation coverage against the product specification, useful during rollout and compliance reviews. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch09: Tenant Admin Audit

**Screenshot:** `screenshots/09-tenant-admin-audit.png`

From the tenant admin perspective: The audit trail records privileged actions across the tenant—who changed what and when—for governance and security reviews. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch10: Tenant Admin Execution

**Screenshot:** `screenshots/10-tenant-admin-execution.png`

From the tenant admin perspective: Execution evidence captures proof that configured workflows and controls were exercised, supporting audits and go-live checklists. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch11: Tenant Admin Users

**Screenshot:** `screenshots/11-tenant-admin-users.png`

From the tenant admin perspective: User accounts creates staff logins for HR, managers, interviewers, and BGC vendors—separate from employee records created through hiring. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch12: Tenant Admin Clients

**Screenshot:** `screenshots/12-tenant-admin-clients.png`

From the tenant admin perspective: Hiring clients represent who you recruit for—internal business units or external staffing clients—each with its own public careers URL. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch13: Tenant Admin Jobs

**Screenshot:** `screenshots/13-tenant-admin-jobs.png`

From the tenant admin perspective: Jobs lets HR create requisitions, define skills and interview rounds, and publish openings to the careers site when ready. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch14: Tenant Admin Candidates

**Screenshot:** `screenshots/14-tenant-admin-candidates.png`

From the tenant admin perspective: The talent pool stores all candidates and parsed resume data, enabling reuse and matching when new jobs are published. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch15: Tenant Admin Applications

**Screenshot:** `screenshots/15-tenant-admin-applications.png`

From the tenant admin perspective: Applications is the ATS pipeline—track status from applied through interview, offer, onboarding, and hired, with interview evidence. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch16: Tenant Admin Employees

**Screenshot:** `screenshots/16-tenant-admin-employees.png`

From the tenant admin perspective: The employee directory lists active staff with export options. HR and admins maintain records; managers get read access to their teams. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch17: Tenant Admin Onboarding

**Screenshot:** `screenshots/17-tenant-admin-onboarding.png`

From the tenant admin perspective: Onboarding manages new-joiner cases after offer acceptance, coordinating documents, policies, and conversion to employee records. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch18: Tenant Admin Preboarding Admin

**Screenshot:** `screenshots/18-tenant-admin-preboarding-admin.png`

From the tenant admin perspective: Preboarding configures the new-hire portal checklist—required documents and policies candidates complete before day one. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch19: Tenant Admin Org

**Screenshot:** `screenshots/19-tenant-admin-org.png`

From the tenant admin perspective: Departments shows the org structure, headcount per unit, and department head assignments used in approvals and exit workflows. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch20: Tenant Admin Exit

**Screenshot:** `screenshots/20-tenant-admin-exit.png`

From the tenant admin perspective: Exit management handles resignations and offboarding—clearance, NOC sign-offs, and separation tasks through the employee lifecycle end. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch21: Tenant Admin Projects

**Screenshot:** `screenshots/21-tenant-admin-projects.png`

From the tenant admin perspective: Projects tracks client engagements and staffing—link employees to billable work for timesheets and manpower planning. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch22: Tenant Admin Manpower

**Screenshot:** `screenshots/22-tenant-admin-manpower.png`

From the tenant admin perspective: Manpower planning views allocation across projects, highlighting bench, utilization, and demand versus supply. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch23: Tenant Admin Assets

**Screenshot:** `screenshots/23-tenant-admin-assets.png`

From the tenant admin perspective: Assets registers laptops, phones, and equipment issued to employees, with assignment and return tracking. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch24: Tenant Admin Leave

**Screenshot:** `screenshots/24-tenant-admin-leave.png`

From the tenant admin perspective: Attendance and leave covers holiday calendars, leave balances, requests, and manager approvals in one module. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch25: Tenant Admin Timesheets

**Screenshot:** `screenshots/25-tenant-admin-timesheets.png`

From the tenant admin perspective: Timesheets capture weekly hours against projects. Employees submit; managers review and approve before payroll consumption. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch26: Tenant Admin Payroll

**Screenshot:** `screenshots/26-tenant-admin-payroll.png`

From the tenant admin perspective: Payroll runs salary processing and payslip distribution. HR initiates runs; employees view their own payslips securely. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch27: Tenant Admin Benefits

**Screenshot:** `screenshots/27-tenant-admin-benefits.png`

From the tenant admin perspective: Benefits manages enrollment in health, insurance, and perk programmes with eligibility rules and employee self-service. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch28: Tenant Admin Expenses

**Screenshot:** `screenshots/28-tenant-admin-expenses.png`

From the tenant admin perspective: Expenses handles reimbursement claims—submit receipts, route for approval, and track settlement status. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch29: Tenant Admin Goals

**Screenshot:** `screenshots/29-tenant-admin-goals.png`

From the tenant admin perspective: Goals supports OKRs and targets aligned to appraisal cycles, visible to employees and their reporting managers. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch30: Tenant Admin Appraisals

**Screenshot:** `screenshots/30-tenant-admin-appraisals.png`

From the tenant admin perspective: Appraisals runs performance review cycles with ratings, feedback, and calibration across the organisation. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch31: Tenant Admin Trainings

**Screenshot:** `screenshots/31-tenant-admin-trainings.png`

From the tenant admin perspective: Trainings catalogues courses, schedules sessions, and tracks completion for compliance and skill development. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch32: Tenant Admin Recognition

**Screenshot:** `screenshots/32-tenant-admin-recognition.png`

From the tenant admin perspective: Recognition enables peer and manager appreciation, badges, and 360-degree feedback to drive engagement. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch33: Tenant Admin Approvals

**Screenshot:** `screenshots/33-tenant-admin-approvals.png`

As a tenant admin, The approvals inbox is a unified queue for leave, timesheets, expenses, and workflow tasks awaiting action. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch34: Tenant Admin Workflows

**Screenshot:** `screenshots/34-tenant-admin-workflows.png`

From the tenant admin perspective: Workflows defines approval chains and automation rules that route requests to the right approvers by designation. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch35: Tenant Admin Notifications

**Screenshot:** `screenshots/35-tenant-admin-notifications.png`

From the tenant admin perspective: Notifications configures email and in-app alerts for key events—offers, approvals, and policy updates. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch36: Tenant Admin Org Masters

**Screenshot:** `screenshots/36-tenant-admin-org-masters.png`

From the tenant admin perspective: Org masters maintains locations, grades, cost centres, and other structural reference data for HR operations. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch37: Tenant Admin Masters

**Screenshot:** `screenshots/37-tenant-admin-masters.png`

From the tenant admin perspective: Masters provides additional lookup tables—leave types, expense categories, and module-specific enumerations. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch38: Tenant Admin Privacy

**Screenshot:** `screenshots/38-tenant-admin-privacy.png`

From the tenant admin perspective: Privacy and consent handles data-subject requests, policy acknowledgements, and GDPR-style rights for employees. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch39: Tenant Admin Profile

**Screenshot:** `screenshots/39-tenant-admin-profile.png`

As a tenant admin, My profile shows the signed-in user's account details, linked employee record, and password preferences. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch40: Tenant Admin Reports Run

**Screenshot:** `screenshots/40-tenant-admin-reports-run.png`

From the tenant admin perspective: Running a report executes the query against live data and displays generated results with timestamps for auditability. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch41: Hr Dashboard

**Screenshot:** `screenshots/41-hr-dashboard.png`

As a HR, The dashboard aggregates live KPIs—open jobs, applications, headcount, and pending approvals—with tiles that drill into underlying data. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch42: Hr Jobs

**Screenshot:** `screenshots/42-hr-jobs.png`

From the HR perspective: Jobs lets HR create requisitions, define skills and interview rounds, and publish openings to the careers site when ready. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch43: Hr Applications

**Screenshot:** `screenshots/43-hr-applications.png`

From the HR perspective: Applications is the ATS pipeline—track status from applied through interview, offer, onboarding, and hired, with interview evidence. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch44: Hr Onboarding

**Screenshot:** `screenshots/44-hr-onboarding.png`

From the HR perspective: Onboarding manages new-joiner cases after offer acceptance, coordinating documents, policies, and conversion to employee records. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch45: Hr Preboarding Admin

**Screenshot:** `screenshots/45-hr-preboarding-admin.png`

From the HR perspective: Preboarding configures the new-hire portal checklist—required documents and policies candidates complete before day one. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch46: Hr Employees

**Screenshot:** `screenshots/46-hr-employees.png`

From the HR perspective: The employee directory lists active staff with export options. HR and admins maintain records; managers get read access to their teams. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch47: Manager Dashboard

**Screenshot:** `screenshots/47-manager-dashboard.png`

As a manager, The dashboard aggregates live KPIs—open jobs, applications, headcount, and pending approvals—with tiles that drill into underlying data. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch48: Manager Employees

**Screenshot:** `screenshots/48-manager-employees.png`

From the manager perspective: The employee directory lists active staff with export options. HR and admins maintain records; managers get read access to their teams. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch49: Manager Org

**Screenshot:** `screenshots/49-manager-org.png`

From the manager perspective: Departments shows the org structure, headcount per unit, and department head assignments used in approvals and exit workflows. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch50: Manager Projects

**Screenshot:** `screenshots/50-manager-projects.png`

From the manager perspective: Projects tracks client engagements and staffing—link employees to billable work for timesheets and manpower planning. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch51: Manager Manpower

**Screenshot:** `screenshots/51-manager-manpower.png`

From the manager perspective: Manpower planning views allocation across projects, highlighting bench, utilization, and demand versus supply. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch52: Manager Leave

**Screenshot:** `screenshots/52-manager-leave.png`

From the manager perspective: Attendance and leave covers holiday calendars, leave balances, requests, and manager approvals in one module. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch53: Manager Timesheets

**Screenshot:** `screenshots/53-manager-timesheets.png`

From the manager perspective: Timesheets capture weekly hours against projects. Employees submit; managers review and approve before payroll consumption. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch54: Manager Benefits

**Screenshot:** `screenshots/54-manager-benefits.png`

From the manager perspective: Benefits manages enrollment in health, insurance, and perk programmes with eligibility rules and employee self-service. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch55: Manager Expenses

**Screenshot:** `screenshots/55-manager-expenses.png`

From the manager perspective: Expenses handles reimbursement claims—submit receipts, route for approval, and track settlement status. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch56: Manager Goals

**Screenshot:** `screenshots/56-manager-goals.png`

From the manager perspective: Goals supports OKRs and targets aligned to appraisal cycles, visible to employees and their reporting managers. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch57: Manager Appraisals

**Screenshot:** `screenshots/57-manager-appraisals.png`

From the manager perspective: Appraisals runs performance review cycles with ratings, feedback, and calibration across the organisation. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch58: Manager Trainings

**Screenshot:** `screenshots/58-manager-trainings.png`

From the manager perspective: Trainings catalogues courses, schedules sessions, and tracks completion for compliance and skill development. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch59: Manager Recognition

**Screenshot:** `screenshots/59-manager-recognition.png`

From the manager perspective: Recognition enables peer and manager appreciation, badges, and 360-degree feedback to drive engagement. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch60: Manager Approvals

**Screenshot:** `screenshots/60-manager-approvals.png`

As a manager, The approvals inbox is a unified queue for leave, timesheets, expenses, and workflow tasks awaiting action. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch61: Manager Masters

**Screenshot:** `screenshots/61-manager-masters.png`

From the manager perspective: Masters provides additional lookup tables—leave types, expense categories, and module-specific enumerations. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch62: Manager Privacy

**Screenshot:** `screenshots/62-manager-privacy.png`

From the manager perspective: Privacy and consent handles data-subject requests, policy acknowledgements, and GDPR-style rights for employees. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch63: Manager Exit

**Screenshot:** `screenshots/63-manager-exit.png`

From the manager perspective: Exit management handles resignations and offboarding—clearance, NOC sign-offs, and separation tasks through the employee lifecycle end. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch64: Manager Profile

**Screenshot:** `screenshots/64-manager-profile.png`

As a manager, My profile shows the signed-in user's account details, linked employee record, and password preferences. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch65: Employee Dashboard

**Screenshot:** `screenshots/65-employee-dashboard.png`

As a employee, The dashboard aggregates live KPIs—open jobs, applications, headcount, and pending approvals—with tiles that drill into underlying data. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch66: Employee Leave

**Screenshot:** `screenshots/66-employee-leave.png`

From the employee perspective: Attendance and leave covers holiday calendars, leave balances, requests, and manager approvals in one module. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch67: Employee Timesheets

**Screenshot:** `screenshots/67-employee-timesheets.png`

From the employee perspective: Timesheets capture weekly hours against projects. Employees submit; managers review and approve before payroll consumption. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch68: Employee Payroll

**Screenshot:** `screenshots/68-employee-payroll.png`

From the employee perspective: Payroll runs salary processing and payslip distribution. HR initiates runs; employees view their own payslips securely. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch69: Employee Benefits

**Screenshot:** `screenshots/69-employee-benefits.png`

From the employee perspective: Benefits manages enrollment in health, insurance, and perk programmes with eligibility rules and employee self-service. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch70: Employee Expenses

**Screenshot:** `screenshots/70-employee-expenses.png`

From the employee perspective: Expenses handles reimbursement claims—submit receipts, route for approval, and track settlement status. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch71: Employee Goals

**Screenshot:** `screenshots/71-employee-goals.png`

From the employee perspective: Goals supports OKRs and targets aligned to appraisal cycles, visible to employees and their reporting managers. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch72: Employee Appraisals

**Screenshot:** `screenshots/72-employee-appraisals.png`

From the employee perspective: Appraisals runs performance review cycles with ratings, feedback, and calibration across the organisation. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch73: Employee Trainings

**Screenshot:** `screenshots/73-employee-trainings.png`

From the employee perspective: Trainings catalogues courses, schedules sessions, and tracks completion for compliance and skill development. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch74: Employee Recognition

**Screenshot:** `screenshots/74-employee-recognition.png`

From the employee perspective: Recognition enables peer and manager appreciation, badges, and 360-degree feedback to drive engagement. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch75: Employee Approvals

**Screenshot:** `screenshots/75-employee-approvals.png`

As a employee, The approvals inbox is a unified queue for leave, timesheets, expenses, and workflow tasks awaiting action. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch76: Employee Privacy

**Screenshot:** `screenshots/76-employee-privacy.png`

From the employee perspective: Privacy and consent handles data-subject requests, policy acknowledgements, and GDPR-style rights for employees. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch77: Employee Exit

**Screenshot:** `screenshots/77-employee-exit.png`

From the employee perspective: Exit management handles resignations and offboarding—clearance, NOC sign-offs, and separation tasks through the employee lifecycle end. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch78: Employee Profile

**Screenshot:** `screenshots/78-employee-profile.png`

As a employee, My profile shows the signed-in user's account details, linked employee record, and password preferences. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch79: Bgc Vendor Portal

**Screenshot:** `screenshots/79-bgc-vendor-portal.png`

From the BGC vendor perspective: This screen covers the portal module. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch80: Bgc Vendor Profile

**Screenshot:** `screenshots/80-bgc-vendor-profile.png`

As a BGC vendor, My profile shows the signed-in user's account details, linked employee record, and password preferences. Notice the application shell: grouped navigation on the left, module header with breadcrumbs and tabs, and the signed-in user with role badges at the bottom.

## ch81: Platform Admin Tenant Provisioning

**Screenshot:** `screenshots-audit/81-platform-admin-tenants.png`

The Platform Admin works outside tenant business operations. This screen creates isolated companies with a unique subdomain, country, and first tenant administrator. The navigation deliberately exposes only platform requirements and tenant provisioning; employee, payroll, recruitment, and other tenant modules are hidden and also rejected by the API guard.

## ch82: Tenant Admin Surveys and eNPS

**Screenshot:** `screenshots-audit/82-tenant-admin-surveys.png`

Tenant administrators and HR can create an engagement survey, choose eNPS or pulse format, set the question and anonymity policy, then open or close the response window. Analytics are calculated from tenant-scoped responses, including promoter, passive, detractor, response count, and final eNPS values. Employees see only open surveys and never receive the administrative analytics controls.

## ch83: Tenant Admin Compliance Case Board

**Screenshot:** `screenshots-audit/83-tenant-admin-compliance.png`

The compliance case board gives authorized administrators a tenant-wide view of employee-raised concerns. Each case carries a type, summary, details, confidentiality flag, status, and resolution lifecycle, while employees are restricted to their own submissions. Retention rules and purge previews are separated from daily case handling so governance actions remain explicit and auditable.

## ch84: Payroll Adjustments and Tax Verification

**Screenshot:** `screenshots-audit/84-tenant-admin-payroll-extras.png`

Payroll administrators can record a bonus, deduction, reimbursement, or arrear adjustment against a real employee and payroll period. The tax declaration queue shows employee submissions and supports a controlled verification step instead of silently changing tax data. Tenant validation prevents an adjustment or declaration from referencing an employee in another company, and employee self-service remains read-only for adjustments.

## ch85: Project Skill Requirements

**Screenshot:** `screenshots-audit/85-tenant-admin-project-skills.png`

Projects now store their own required skill set rather than borrowing unrelated job-requisition skills. Administrators define the project code, manager, dates, location, and comma-separated skill requirements, which are persisted as structured values. These requirements become the source of truth for bench matching and allocation decisions on the manpower screen.

## ch86: Manager Bench and Skill Matching

**Screenshot:** `screenshots-audit/86-manager-bench-skill-match.png`

Managers can inspect active employees with remaining allocation capacity, including current utilization and available percentage. Selecting a project runs a live comparison between project-required skills and employee skills, producing matched and missing lists plus a percentage score. The manager can allocate a suitable person from the result, while server-side rules prevent total active allocation from exceeding one hundred percent.

## ch87: Employee Survey Response

**Screenshot:** `screenshots-audit/87-employee-survey-response.png`

Employees receive only surveys that are open for their tenant and can submit one validated response per survey. eNPS uses the standard zero-to-ten score with an optional comment, and the server links eligibility to the authenticated employee rather than trusting a supplied employee identifier. Administrative create, close, and analytics controls are absent from this role.

## ch88: Employee Compliance Reporting

**Screenshot:** `screenshots-audit/88-employee-compliance-case.png`

The employee compliance workspace provides a clear route to report policy, privacy, conduct, or other concerns and optionally mark the case confidential. The My Cases section tracks only that employee's submissions and resolution state. The tenant-wide case board, retention configuration, and purge tools are not rendered for employees and are protected separately at API level.

## ch89: Employee Payroll Self-Service

**Screenshot:** `screenshots-audit/89-employee-tax-and-adjustments.png`

Employees can review only their own payroll adjustments and submit structured tax declarations for a selected financial year and tax regime. Investment, insurance, housing, and other declared amounts are entered as data and retained with a submission and verification status. Employees cannot create adjustments, list other workers' declarations, or verify their own submission.

## ch90: Interviewer Assigned Applications

**Screenshot:** `screenshots-audit/90-interviewer-assigned-applications.png`

The interviewer view is assignment-scoped: only applications with an interview round assigned to the signed-in interviewer are returned. The role can open the candidate context and record the interview result with required evidence, but cannot schedule rounds, move pipeline status, publish jobs, or administer hiring clients. This keeps evaluation responsibility separate from recruiting control.

## ch91: Interviewer Candidate Scope

**Screenshot:** `screenshots-audit/91-interviewer-assigned-candidates.png`

The interviewer talent pool is derived from assigned applications rather than the full tenant candidate database. Candidate identity and relevant interview context remain available for evaluation, while unassigned candidates are excluded by the backend query. The reduced navigation reinforces that the interviewer is a participant in selected rounds, not a general recruiter.

## ch92: Second Tenant Data Isolation

**Screenshot:** `screenshots-audit/92-second-tenant-isolation.png`

A separately provisioned second tenant opens the same candidate module with an empty dataset, even though the first tenant has a populated recruitment flow. Tenant identity is taken from the authenticated session and checked against the request header, and cross-tenant identifiers are rejected across employees, benefits, assets, goals, files, and interview assignments. This screen is the visible proof of the database-level isolation exercised by the automated lifecycle test.
